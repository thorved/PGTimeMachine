# Server Configuration
PORT=8080
GIN_MODE=debug

# Backup Configuration
BACKUP_DIR=./backups

# PostgreSQL Tools Path (optional, if not in PATH)
# PG_DUMP_PATH=/usr/bin/pg_dump
# PSQL_PATH=/usr/bin/psql

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
